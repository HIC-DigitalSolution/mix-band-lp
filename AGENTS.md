# MIX交流上等バンドLP — agent contract

このファイルが正典です。Codex はこれを直接読み、Claude Code は `CLAUDE.md` から
ここへ来ます。**内容を二重に持たないでください。**片方だけ更新されると、
どちらが古いのか誰にも分からなくなります。

## Project

ASOBIBAR（マッチングバー）のイベント「**ミックス交流上等バンド**」の**単独LP**です。
出力は静的 HTML / CSS（必要なら JS）。フレームワークは使いません。
**ビルドは Sass のコンパイルだけです**（`node scripts/build.mjs` → `css/style.css`）。
**`index.html` は生成物ではありません。手で書きます。**
編集入口は `src/index.html`、本文は `src/sections/`、スタイルは `src/scss/`。
`npm run build` でルートの `index.html` と `css/style.css` を生成します。生成物は直接編集しません。

**2026-09-08、依頼者はカンプ作業を一旦止め、`design/mix-lp-content-design-comp-17.dc.html` を基準にClaude Codeでの実装へ移ると決めました。公開前の最終確認は未完了です。**

- 元は Y2K NIGHT LP の1セクション（`#mix`・P.04）でした。そこから独立させたものです
- 旧実装・実測・差し戻しの経緯は `../完了タスク (LP)/asb_lp_Y2K/`（**別リポジトリ**。
  このマシンにしかありません）。`docs/gotchas/guessed-genre-from-a-word.md` と
  `docs/exec-plans/active/mix-band-section.md` に中身があります
- テストもステージングも無い。**壊れても差分では見えず、公開してから気づきます**
- 公開先は未定（Y2Kは GitHub Pages、19歳LPは Vercel）

## 何が必ず走るか（誰が操作していても）

読む人にしか効かない文章と違い、これらは git hook で実行されます。

```bash
node scripts/harness/harness-check.mjs                       # 差分に対する不変条件
node scripts/harness/harness-run.mjs --stage pre-push --dry-run
node scripts/harness/new-plan.mjs task <slug> --title "..."  # exec-plan / adr / gotcha
```

- `harness/contracts/mix-lp.yaml` — **このLP固有の不変条件**（実際に事故った箇所だけ）
- `harness/scenarios/design-comp.yaml` — **カンプを起こす順番**（`manual`）
- `harness/scenarios/static-html.yaml` — 公開前の構文チェックと404チェック（`pre-push`）
- `.githooks/` — clone ごとに1回 `sh scripts/install-hooks.sh`

**作業完了を報告する前に `harness-check.mjs` を走らせてください。**読み取り専用です。

`[manual]` と出る行は、機械が判定できない規則です。**あれが確認事項の本体**で、
飾りではありません。毎回 `manual` と出続けます。行数は追加した規則に応じて変わるため、実際の出力を正とします。

### 共通部分は、このリポジトリの外にあります

ハーネスは**エンジンと汎用ルールを他プロジェクトと共有**しています。実体は
`../`（`Job/LP/`）に1部だけあり、ここからは symlink で参照しています。

| 参照（symlink） | 実体 |
| --- | --- |
| `scripts/harness/` | `../../scripts/harness` |
| `harness/README.md` | `../../harness/README.md` |
| `harness/contracts/{workflow,secrets,lp-design}.yaml` | `../../../harness/contracts/` |
| `harness/scenarios/{workflow,harness-self-test}.yaml` | `../../../harness/scenarios/` |
| `docs/README.md`、`docs/exec-plans/{README,template}.md`、`docs/*/_template.md` | 同じ相対位置 |
| `docs/design-docs/lp-{cvr,visual}-knowledge.md` | `../../../docs/design-docs/` |

**このLP固有なのは実ファイルのものだけ**です:
`harness/contracts/mix-lp.yaml`、`harness/scenarios/{design-comp,static-html}.yaml`、
`tools/`、`AGENTS.md`、`.claude/`、`.codex/`、`.githooks/`。
LPのルールを足すならここに書いてください。**symlink 側を編集すると、それを参照している
他のプロジェクト全部に即座に効きます。**（`.claude/settings.json` の `deny` で
主要なものは塞いでありますが、塞いでいない経路もあります。）

**`git clone` しただけでは動きません。** symlink はリポジトリの外を指しているので、
共有元が無い環境では全部 dead link になり、`harness-check.mjs` はモジュールが
見つからず起動しません。hook を入れていれば、その時点でコミットも通らなくなります。

```sh
for l in $(find . -type l); do [ -e "$l" ] || echo "DEAD: $l"; done    # dead link を先に確認する

SHARED=/path/to/lp-harness                     # 共有元（このマシンでは ..）
for l in $(find . -type l); do
  rm "$l" && cp -R "$SHARED/${l#./}" "$l"
done
```

戻したあとは `node scripts/harness/harness-check.mjs` が動くことを確認します。
**この状態でコミットすると symlink が実ファイルに戻った差分が乗ります。**
共有構成を維持するなら、その差分は捨ててください。

## いま決まっていること

**イベントの核として依頼者が確定させた事実は次の4点です**（2026-09-07）。

| | |
| --- | --- |
| 組数 | **最大4〜6組**のグループが交差する（旧「最大4組」は誤り） |
| 開催日 | **金・土以外は開催期間中ずっと。** バナーの「1日限り」は1晩が完結するという意味 |
| 仕組み | 「**チャイムが鳴る**」システムで、**そこからお題が出される** |
| 位置づけ | **恋愛リアリティ体験**と言ってよい |

**LPへ必ず入れる訴求は、依頼者が指定した次の3語です**（2026-09-07）。

- **恋愛リアリティ体験**
- **最大4〜6組**
- **日〜木曜日限定**

支給バナー内では小さく読めないため、3語ともHTMLの読める文字として再掲します。
FV直下で3点を短く提示し、その後のイベント理解で「恋愛リアリティ体験」、仕組みで
「最大4〜6組」、開催情報で「日〜木曜日限定」を詳しく伝えます。
「日〜木曜日限定」には、具体的な開催期間が確定後に「開催期間中」を併記し、
通年開催のようには見せません。バナーの「1日限り」はHTMLの訴求へ流用しません。

**2026-09-08に、内容構成へ使う情報が追加されました。**

| | |
| --- | --- |
| 対象 | **20〜30代の男女** |
| 来店前の期待 | **いつもと違う刺激がほしい／関係が動くきっかけがほしい／非日常を体験したい** |
| イベント説明 | **恋愛リアリティショーをASOBIBARで行っている** |
| 開催期間 | **2026年10月1日〜10月31日** |
| 参加条件 | **オレンジバンド** |
| 開催店舗 | 全店。本文に一覧は載せず、CTAの店舗選択ダイアログ内だけに表示する |
| CTA | 店舗選択ダイアログを開き、選んだ店舗の**公式LINE**へ移動する |
| 体験価値 | 恋愛リアリティ体験、ASOBIBARでの感動体験 |
| 目指す結果 | 楽しむ、感動する、カップル成立、再来店 |

「カップル成立」は保証表現にせず、関係が動くきっかけとして表現します。
「再来店」は参加者のメリットにせず、運営側の成果として扱います。
料金・受付時間・実績・お客さまの声は第一稿に載せません。
入店時刻、年齢・本人確認、キャンセル規定は、回答が必要になった時点まで
`TODO(依頼者確認)`のまま残します。

現在の内容構成は `docs/design-docs/content-structure-first-draft.md` を正とします。
ダイアログへ表示する店舗名と各公式LINE URL、LPへの流入元、目標CVRは未決定です。
これ以外の情報も推測で足さないでください。

**イベントの中身は依頼者の判断であえて伏せています。**「チャイムが鳴るまで分からない」
が訴求の軸なので、中身を書くと軸ごと壊れます。

### 配色の方向（2026-09-07・依頼者の指示）

**ゼブラ柄 × ド派手なピンク**、ヤンキーカルチャーを象徴する**ネオンカラー**。
モチーフは**ド派手なピンクとゼブラ柄の女子部屋**。

**これは依頼者が出した方向です。実装側の推測ではありません。**
（Y2Kで「上等」の一語から「ヤンキー漫画」と推測して差し戻された経緯があるため、
出どころを明記しています。混同しないこと。）

**2026-09-07、世界観は「ヤンキーカルチャーのネオン」へ寄せる方向になりました。**
ハイギャルのラグジュアリー方向は採用しません。
**リボンは採用しない方向で設計中です。**新しいカンプに既存のリボン素材を使わず、
MIXは暗い紫からピンクへ移る光の領域の重なりで表す案を先に検討します。
リボン素材は過去案の記録なので、最終確定までは削除しません。

**配色の役割は次のように決定しました。**

1. **CTAはネオンピンク `#FF2D95`。** 支給バナーがほぼ持たない色相で、べた塗りはCTAだけに残します。
2. **読ませる面は白またはスモーキーピンク。** ゼブラ柄の上に本文は乗せません（`lp-visual-knowledge` §4
   「読ませたい場所に世界観を持ち込まない」）。料金・注意書き・開催日を置く
   「静かな面」を強い面の間に確保します。

**2026-09-07、依頼者から「ワクワクする配色にしたい」「バナーは一旦仮置き」と指示がありました。**
比較05では広いコバルトブルー、ネオンピンク、オレンジが同じ強さで競合し、
サイバーゲーム寄りに見えました。`design/mix-lp-cohesive-neon-color-study-06.png` では、
濃紫→電気的な紫→ラズベリーピンクへ整理し、ネオンピンクはCTAと細線、オレンジは点だけに
しています。**2026-09-08、依頼者が比較06のデザインを一番良いと判断し、採用方向になりました。**
支給バナーの色へページ全体を従わせず、バナーを外してもページ側にワクワク感が残ることを
判断基準にします。

**オレンジは補色として使いたい、という依頼者の方向が追加されました。**
`design/mix-lp-orange-complement-color-study-07.png` では、チャイムの場面だけを深い
ブルーバイオレットにし、オレンジの光を一度だけ大きく重ねています。ほかの面では小さな点と
短い罫だけに反復し、CTAはネオンピンクのままです。2026-09-07にはこの方向から
ワイヤーフレームへ進みましたが、**2026-09-08のカンプ確認で比較06へ戻す判断が出ました。**
比較07は判断の経緯として残します。

### 配色ガイド「ハイギャル（黄色×ピンク）」（2026-09-07・依頼者から提示・**方向としては不採用**）

出典は依頼者から渡された `docs/design-docs/high-gal-color-guide.md`
（原本は `~/Downloads/`。**当日 `~/Desktop/template/` が消えた実績があるため写しをリポジトリに置いています。**）
2026-09-07、世界観をヤンキーカルチャーのネオンへ寄せることになったため、
**ラグジュアリーとマスタード主体の方向は採用しません。**資料は判断の経緯として残します。

要点は4つ ——
① 主役のイエローは**マスタード**（原色の黄は禁止）／
② 広面積のピンクは**低彩度のスモーキーピンク** `#E2C2C6`、高彩度ネオンピンクは**点・線だけ**／
③ ネオンの線光とゴールドの光沢をレイヤーで足す／
④ 視線の集まる中央と**可読エリアは白（大理石調）**で色彩ノイズをリセット。
柄は**メイン2色をブレンドしたゼブラ**にして新しい色を増やさない。

方向は採用しませんが、②「彩度を面に広げない」と④「読ませる面を白で確保する」は、
CTAと可読性の共通原則として残します。

**不採用の理由と、流用しない要素:**

- **主役がピンクからマスタードに入れ替わります。** 9/7 の指示は「ド派手なピンク」が主役、
  このガイドはピンクを低彩度の地に降ろして主役をマスタードに置きます。**別の方向です。**
  今回はド派手なピンクとヤンキーネオンを採ります。
- **マスタード `#D97706` は 彩度×明度 = 0.827。** 却下された Y2K の地 `#003ed6`（0.839）と
  ほぼ同じ強さです。インテリアの「主役」は面積の小さい家具ですが、**これをLPの
  ヒーローの地に広げると Y2K と同じ差し戻しになります。**マスタードは面積を限定します。
- **ネオンピンク `#FF2D95` も 0.824 で、マスタードと同じ強さです。**同じ強さの2色では
  CTAが勝てません。**どちらか一方をCTA専用にして、他の場所でべた塗りに使わないこと。**
- **ネオンは明るい地では光りません。** `#E2C2C6` は明度の高い地です。③の「線状の光」は
  暗い面がないと成立しないので、**暗い面を1セクションだけ持たせる**のが筋です。
  そこがページで一番強い面になるので、CTAはそこに置きます。白・大理石は読ませる面へ。
- **文字色。** 白文字のコントラストは マスタード 3.19:1／ネオンピンク 3.46:1 で、
  **本文には落ちます**（大きい文字とUI部品の 3:1 は満たす）。CTAの文字は**黒**が前提。
  ゴールドのグラデ文字は可読性が読めないので使いません。
- ガイドの狙いは「子供っぽさの排除・ラグジュアリー」で、
  9/7 の「ヤンキーカルチャーを象徴するネオン」とは**別の世界**です。
  `lp-visual-knowledge` §1 に従い、二つを混ぜません。
- ガイド §4 のプロンプト雛形は **Tailwind 前提**です。本番は静的HTML+CSSなので、
  **使うのは hex と配分比率だけ**にします。

**参照の実物は存在しません（2026-09-07・依頼者が明言）。**
「ド派手ピンクとゼブラ柄の女子部屋」は**特定の部屋・作品を指していません。**
依頼者のイメージの言葉です。**画像を探しに行かないこと。**

これは `reference-seen-before-genre` の前提が外れたのではなく、**形が変わった**ということです。
Y2K の事故は「実物があるのに見ずに推測した」でした。今回は原本が無いので、
**突き合わせる相手は依頼者の頭の中の像しかありません。**
そこで突き合わせを**入力側から出力側に移します** —— 
**コピーを入れず、面積比だけの配色見本を1枚先に見せて、ヤンキーネオンの強さをそこで合わせる。**
カンプ本体まで進んでから世界観がずれると、Y2K と同じ量を捨てます。

なお、このガイドは配色のロジックで、参照の実物ではありません。
本文の出典番号 `[2]`〜`[8]` は**本体が付いていない二次資料**です。

### 参考サイト3件（2026-09-07・依頼者が挙げたURL）

**3件とも実際にブラウザで開いてDOM・CSS・素材を読み、技法を取りました。**
画面写真からの推測ではありません。**取った内容と数値は
`docs/design-docs/reference-sites.md` に全部あります。**

| | 依頼者の言葉 | 取ったもの |
| --- | --- | --- |
| `kbc.co.jp/70th/` | 所々オブジェクトが浮いている。**交流がMIXする**意味に使えそう | 浮遊を採用する場合の3層構造・振幅±3〜5%・duration全違い/delay 0・`alternate-reverse`。リボン形状は採用しない方向 |
| `futuretrain.jp` | **配色の移り変わり**と、Future Imagination Courseの**カードをスライドして読む流れ**を意識したい | 端点だけ決めて中間は補間から取る。概要は選択肢のレール→同じ枠の詳細パネル→次カードへ進む構造を3枚へ翻訳する。新しい色・固有素材は足さない |
| `on-the-trip.com/sound-trip` | **サイトの構成をベースにしたい。配色の使い方と図形の配置が好き** | 1色の広い地＋巨大な有機形＋小さな端装飾、境界の10〜25%の食い込み、中央の可読域、大小1組の非対称配置、終盤でFVの暗色へ戻す構成 |

**ハーネスに入れてあります**（`mix-lp.yaml`）——
`floating-objects-follow-one-physics`、`section-colors-come-from-one-ramp`、
`sound-trip-composition-frames-the-page`（manual）、
`futuretrain-course-slide-grammar`（manual）、
`reduced-motion-is-handled` と `color-ramp-tokens-exist`（機械）。

#### ランプ（カンプ10・2端点6ステップ）

`#24102E`（濃紫の夜）→ `#FF2D95`（ネオンピンク）。
`#6338D4`（電気的な紫）は光が交差するネオン線、`#FF8A1F`（明るいオレンジ）は
火花・点・短い罫だけに使い、どちらも広い地のランプへ混ぜません。

| | hex | 彩度×明度 | 用途 |
| --- | --- | --- | --- |
| 01 | `#24102E` | 0.118 | FV・CTAの暗い地 |
| 02 | `#5B163F` | 0.271 | 仕組みの暗い面 |
| 03 | `#8D1B58` | 0.447 | 境界の有機形 |
| 04 | `#C91F70` | 0.667 | イベント理解の広い光 |
| 05 | `#E52683` | 0.749 | ネオンの線・小面積 |
| 06 | `#FF2D95` | 0.824 | **CTAだけ** |

**CTAがページ最強面である根拠は「べた塗りの面積」と、支給バナーがほぼ持たない色相です。**
バナーのオレンジは 0.878 でCTAのネオンピンク 0.824 より強いため、彩度×明度だけでは保証できません。
**読ませる面（淡いピンク `#F2D6DF`・白 `#FFF7FA`）はランプに乗せません。**

**マスタード `#D97706`（0.827）はオレンジと役目が重なるため採用しません。**
配色ガイド①「主役はマスタード」からは、ここだけ外れています。

#### カンプ

2026-09-08、配色比較06へ戻した内容カンプ09を基に、依頼者の指示で
SOUND TRIPの背景構成を再確認した `design/mix-lp-content-design-comp-10.dc.html` を
作成しました。続けてParadox Live 5th Anniversaryを参考にFVを横幅いっぱいへ広げる
`design/mix-lp-content-design-comp-11.dc.html` を作成し、続けて左右からグラスを寄せる男女の
上半身シルエットを加えたカンプ12、人物の頭・髪・肩・腕・手が読めるポーズへ見直したカンプ13を経て、
SOUND TRIPのように大きな有機形の端へ小さな人物・波線・乾杯記号をまとめた
カンプ14を作成し、WHAT IS MIX左下を有機形・平行な輪郭線・小さな点へ整え、YOUR NIGHT横の斜め文字を
削除したカンプ15を経て、依頼者支給の `design/assets/mix-toast-couple.svg` を人物レイヤーへ差し替えた
`design/mix-lp-content-design-comp-16.dc.html` を作成し、チャイムセクションへFV素材をトリミングした仮写真を
`design/assets/mix-band-chime-photo.webp` として追加した `design/mix-lp-content-design-comp-17.dc.html` を実装基準にしています。カンプ13の大きな人物
レイヤーと、調整前のカンプ14〜16は比較用に残します。
カンプ10の全体描画結果は `design/mix-lp-content-design-comp-10.png`、決定は
`docs/design-docs/content-design-comp-10.md`、FV11の決定は `docs/design-docs/content-design-comp-11.md`、
乾杯モチーフの決定は `docs/design-docs/content-design-comp-12.md`、人物ポーズの見直しは
`docs/design-docs/content-design-comp-13.md`、SOUND TRIPのスケールへ戻した人物記号の決定は
`docs/design-docs/content-design-comp-14.md`、端装飾の見直しは
`docs/design-docs/content-design-comp-15.md`、支給SVGの採用は
`docs/design-docs/content-design-comp-16.md`、チャイム写真の追加は
`docs/design-docs/content-design-comp-17.md` です。2026-09-08にカンプ作業を一旦止め、実装へ移行しました。

実装引き継ぎ用の `img/mix-lp-content-design-reference.{pdf,webp}` は、現環境でカンプ17を
一枚へ書き出せなかったため、**カンプ10の描画結果から作った補助資料**です。最新版の画として
扱わず、カンプ17のソースとカンプ11・15・16・17の決定文書、
`docs/design-docs/overview-motion-spec.md` を併読します。本番からこのPDF・WebPを参照せず、
公開前に `img/` から外すか公開対象から除外します。

カンプ10では、6区間に別々の専用SVGを置き、大きな色面、中間の輪郭線・ゼブラ、
小さな火花・点の3段階で背景を組みました。巨大なアウトライン文字は端で切り、本文中央を
空けています。配色、内容順、支給バナー、CTA動線はカンプ09から変更していません。

`design/mix-lp-comp-0{1..7}.dc.html` は**却下済みの記録**です
（差し戻しの理由は `docs/exec-plans/completed/reference-sites-into-harness.md`）。
**同じ案を2回出さないために残しています。**

引き継ぎは `design/README.md`。数字が出ている決定はそこにあります ——
**CTAをオレンジにしない理由**（バナーの実測）、**見出しは375px幅で24pxが上限**、
**過去案のリボンの形**（`design/assets/ribbon-sprite.svg`、新案では不採用の方向）。

現在の内容設計は `docs/design-docs/content-structure-first-draft.md`。
`design/mix-lp-content-funnel-wireframe-10.png` は2026-09-08の回答を反映する前のワイヤーフレームです。
依頼者が提示した
`design/references/content-funnel-order-reference.png` を構成の参照とし、
**誰に？・キャッチ → 共感・理解 → メリット → 仕組み → 実績・証拠 → お客さまの声 →
オファー → 限定性 → CTA**へ対応させています。参照画像の色・ピル・矢印は流用しません。
現在は実績、声、独立した限定性を外す判断が出ています。料金と受付時間も載せません。
店舗一覧は本文へ置かず、CTAの店舗選択ダイアログ内だけに表示します。
画像上の英語は識別ラベルで、本番コピーではありません。日本語の内容は
`docs/design-docs/pre-code-design-blueprint.md` を正とします。

### キーアート

**支給された完成バナー1枚を、現段階ではFVへ仮置きします**（2026-09-07・依頼者指示）。
Y2Kでは `MIX-BAND.png` 1.1MB → `mix-band-key.webp` 132KBに変換して使いました。
**ロゴもコピーも画像に焼き込まれている**ので、HTML側に文字段を持たせると二重になります。
仮置きを実装する場合も生成画像内の再現を使わず、支給ファイルそのものを置きます。
焼き込みの文言は `alt` に全部入れます。

**SPでは焼き込みの小さい2行が約8pxになって読めません。**読ませる必要のある事実は
HTMLで別に置きます。**文字なしのバナーが来たらHTMLで組み直せます。**

最終キーアートとして支給物を残すのか、別素材で組み直すのかは未決定です。
配色は支給バナーへの一致だけで固定せず、バナーを外してもページ側で高揚感が成立するか確認します。

## まだ決まっていないこと（カンプ前に決める前提）

`harness/contracts/lp-design.yaml` が毎回 `manual` で出す9〜10行がこれです。
**デザインの好みではなく前提**で、あとから開くと構成ごとやり直しになります。
依頼者・店舗へ確認したい具体項目と回答欄は
`docs/design-docs/information-request-checklist.md` にまとめています。

- **CVは店舗ごとの公式LINEへの遷移。** ページ上のCTAで店舗選択ダイアログを開き、
  選んだ店舗の公式LINEへ移動します。店舗名と各URLは支給待ちです
- **対象と来店前の期待は確定。** 20〜30代の男女で、いつもと違う刺激、関係が動く
  きっかけ、非日常を求めています。どの広告・投稿から来るかは未決定です
- **目標CVR。** 単価と想定流入から先に置きます。無いと公開後に良否を判定できません
- **10要素のうち何を欠かすか、その理由**
- 公開先とドメイン（`og:image` / `og:url` は絶対URLでないとクローラが拾いません）

## 「いつもの夜を予定外に」はスクロールで重なるカード（2026-09-08・依頼者の指定）

futuretrain.jp の Future Imagination Course と同じ作りです。**JavaScriptは使いません。**

1枚目が `position: sticky` で上に貼り付き、2枚目以降が `clip-path` を持って
その上をせり上がって覆います。**3枚とも常にDOMに在る**ので、
読み上げ・検索・JS無効でも全部読めます。

**セクションを `overflow: hidden` にしないこと。**`hidden` はスクロールコンテナを作るので
中の `sticky` が死にます。**`clip` を使います**（切り抜きは同じように効きます）。
このプロジェクトで2回踏んでいます（`docs/gotchas/sticky-was-declared-not-verified.md`）。

カード間に隙間を空けない、カードをビューポート丸ごとの高さにしない（1行＋1文なので白が余る。
実測 `min(42svh, 268px)`）。仕様は `docs/design-docs/overview-motion-spec.md`、
機械の見張りは `stacked-cards-need-overflow-clip` と `slider-has-no-autoplay`、
言葉の版は `stacked-cards-appear-on-scroll`。

**イベント概要（01 WHAT IS MIX → 02 CHIME → 03 CROSS）はスライドにしていません。**
確定事実は独立したセクションで順に読ませる構成が確定しているので、重複するためです。

## PC幅のレイアウトを持っています（2026-09-09・依頼者の指示）

**`lp-visual-knowledge` §10「PCで横に広げない。カラムはSP幅のまま、余った幅は外周の装飾にする」から
意図的に外れています。**依頼者の指示（「構造は futuretrain と一緒にしたほうがいい」
「まずはLPサイズを作ってその後にSPサイズ」）で、PC幅のレイアウトを作りました。**戻さないこと。**

| | SP | PC（`min-width: 900px`） |
| --- | --- | --- |
| コンテナ | 430px | **1280px**（左右80px） |
| 英字ラベル | 10px | **58px** |
| 本文 | 13px | **18px / 行間1.9 / 字間0.05em** |
| 並べ方 | 縦積み | **横並び**（FV・CHIME・YOUR NIGHTのカード） |

**数値は futuretrain.jp を1440幅で開いて実測したもの**です
（向こうはコンテナ1280・英字96px・和文32px・本文18px/1.9/0.9px・2カラムのブロックが69個）。
**節は増やしていません。**背骨は既に一致していて、違うのはこちらが意図的に落とした節だけです。

**支給バナー（750x583）を原寸より拡大しないこと。**PCでは列を720pxで止めています。

メディアクエリは**各セクションのパーシャルに書きます**（class と `_<名前>.scss` を1対1に保つため）。
機械の見張りは `pc-layout-exists`、言葉の版は `pc-grammar-comes-from-futuretrain`。

## 公開（2026-09-08）

**リポジトリは public、GitHub Pages で配信しています** ——
`https://hic-digitalsolution.github.io/mix-band-lp/`。
public にしたのは依頼者の指示（GPTに読ませるため）。
**カンプ・支給素材・`docs/` の社内向け記録も、リポジトリとしては全部公開されています。**

**配信するのは `index.html` / `css/` / `img/` / `js/` だけ**（`.github/workflows/pages.yml`）。
リポジトリ全体を配信しません。理由は2つ ——

1. `upload-pages-artifact` は **`tar --dereference`** で固める。
   **symlink 15本は共有元（外）を指していて、クローン先には存在しない。**
   リポジトリ全体を渡すと tar が実体を辿れず exit 1 で落ちる
   （`docs/gotchas/pages-needs-nojekyll.md`）。
2. `docs/` と `design/` はサイトとして配信する必要がない。

**このワークフローは main への push で自動的にデプロイします。**
AGENTS.md の「push/deploy は自動で走らせない」から**意図的に外れています。**
外してよいと判断した理由 —— **配信しているのは `noindex` の確認用ページ**で、
店舗リンクも料金も未確定のまま。**本番公開はこれとは別の手順**です
（`noindex` 解除・`og:image` / `og:url`・独自ドメイン。
`og-image-and-index-before-release` が毎回出します）。
**そこは人が判断してから。**

## 出口を3つ置いています（2026-09-08・依頼者の指示）

**`lp-design.yaml` の `exit-routes-not-added` は manual で毎回出ますが、外さないでください。**
依頼者の指示で、ヘッダーに**公式サイトへの出口を意図的に3つ**置いています ——
ロゴ → `asobibar.net`、採用情報 → `/recruit`、店内状況 → `/shops`。
全部 `target="_blank" rel="noopener noreferrer"` で、**LPは閉じません。**

ヘッダーは**公式HPと同じ作り**にしています。実物のCSSから取った値 ——
白地 / sticky / padding 12px / ロゴ高さ20px / ボタンは 10px・700・padding 8px・角丸 4px /
採用情報 `#ffe207`＋黒文字＋`wiggler`（2.5s・回転しながら `#e1007d`／文字 `#ffd488` に一瞬振れる）/
店内状況 `#AA8232`＋白文字。

**ロゴは白地用に暗色版を作りました**（`img/asobibar-logo-dark.svg`。
支給された明色版の文字色を公式サイトと同じ `#070304` に置き換えたもの）。
**明色版（`asobibar-logo-light.svg`）は白地では消えます** ——
文字が `#fff7fa` で、赤いRと多色のIだけ残ります。実際に両方の地で描画して確認済み。

**公式HPのモバイルには、画面下に固定の緑帯**（`#22C55E`「LINEのご予約はこちら」）があります。
このLPでは代わりに**FVに「予約はこちら」**を置きました（依頼者の指示）。
どちらも行き先は店舗選択ダイアログ → 各店舗の公式LINE。

## ページの持ち方（2026-09-08・依頼者の指示）

**依頼者本人が開いて読める形にしておくのが要件です。**効率や DRY を理由に崩さないでください。

| | |
| --- | --- |
| 構造 | **`index.html` 1枚を上から読めば全部わかる。**include もテンプレートも挟まない |
| 区切り | セクションごとに区切りコメント（`hero` / `about` / `schedule` / `footer`） |
| スタイル | **セクションごとに `src/scss/sections/_<名前>.scss`。**`src/scss/style.scss` が `@use` でまとめる |
| 対応 | `section` の **class と Sass パーシャル名を1対1**にする（`.hero` なら `_hero.scss`） |
| 生成物 | **`css/style.css` だけ。**`npm run build`。手で編集しない |
| 画像 | **本番で使うものは `img/`。**`design/` はカンプの作業場で、本番から参照しない |

**セクションを増やす手順:** `index.html` に区切りコメント付きで足す →
同じ名前の Sass パーシャルを作る → `style.scss` に `@use` を1行足す → `npm run build`。

機械が見張っています（`mix-lp.yaml`）——
`index-html-is-written-by-hand` / `styles-are-per-section-sass` /
`css-is-compiled-not-handwritten` / `sass-change-ships-its-css` /
`production-images-live-in-img` / `image-files-live-in-img`。
言葉の版は `page-structure-is-readable-by-the-client`（manual）。

## このプロジェクト固有の作法

**カンプ先行です。順番は `harness/scenarios/design-comp.yaml` にあります。**

- **依頼者が参考にしている実物を見るまで、ジャンルと世界観を決めない。**
  作品名・店名・商品名が出たら、**画像を1枚もらう。**「たぶんこういう系」で
  手を動かし始めない。**このプロジェクトで一度やって全部捨てています**
  （`mix-lp.yaml` の `reference-seen-before-genre`）。
  **ただし「女子部屋」は参照ではありません**（上記・2026-09-07）。原本が無い方向は、
  **配色見本を先に見せて出力側で突き合わせます**
- **見た目の成果物は、人に見せる前に描画して自分の目で見る。**
  `tools/render.sh <file> [幅]` で PNG にします。**2026-09-07 に依頼者から明示指示があり、
  同日3回続けて差し戻しています**（`mix-lp.yaml` の `output-is-rendered-and-seen`、
  `docs/gotchas/read-the-css-not-the-artwork.md`）。
  **ブラウザペインは隠れていると白く写るので確認に使えません**
- **参考がURLなら、CSSと素材画像の両方を見る。**CSSは振る舞いしか語りません。
  **形・塗り・質感・スケールは素材にしか無い**
- **支給素材がFVを占めるなら、色相を実測してからCTAの色を決める。**
  バナーの実測は赤〜橙で 78.8%、ピンクは 4.5%（`docs/design-docs/reference-sites.md` §4）。
  **CTAはバナーが持っていない色相から選びます**
- **推測でやったことは、推測だと書く。**コードコメントに「〜の見え方を借りる」と
  書くなら、**何を見たのか**を書く。見ていないなら「未確認」と書く。
  断定的なコメントは、次の人がそれを検証済みの事実として扱います
- **カンプは `design/` に `.dc.html`。決定は `docs/design-docs/`。**
  カンプは仮の文言でよく、本番HTMLでは許しません（`mix-lp.yaml` で機械が見ています）。
  **カンプは成果物ではありません。成果物は決定です**
- **パロディの記号はジャンル一般のものだけ。**白抜き太ゴシックのテロップ帯、
  エピソード表記、ナレーション調のコピーは使えます。**実在番組のロゴ・タイトルの
  字組み・キーアート・配信社のマークは使いません。**商用ページで実在ブランドを
  流用すると、パロディの意図でも商標の問題になります。
  **この線引きは実装側の判断で、法務確認は取っていません**
- **却下済み（再提案しない）**: 方向A「全部ウィンドウの中」（2026-09-04、MIXで
  先行実装 → 同日「デザインがダメすぎる」で差し戻し。ウィンドウ枠・シークバーは
  デスクトップの言語で、寄せたかったバナーの言語ではなかった）／Reggae One と
  金の刺繍表現・傾けた詰めた字（推測が出発点だった）／地に `#003ed6` のべた塗り
- **測るのはヘッドレスの実機Chrome、見るのはブラウザペイン。**ペインは遅延画像の
  読み込み中に描画が追いつかず、スクロール中のコマが白く写ります。測定スクリプトでは
  先に `document.documentElement.style.scrollBehavior = "auto"` を入れること
  （`scroll-behavior: smooth` と競合して着地前の値を読みます）
- 確認は `file://` ではなく `http://`。静的サーバは `.claude/launch.json` の `mix-lp`

## Non-negotiables

- **`scripts/harness/` `harness/scenarios/` `.githooks/` の編集は、hook が入っている
  全端末での任意コード実行と同じです。**シナリオの `run:` は誰にも見せずに実行される
  シェルです。設定ではなく hook としてレビューしてください。`tools/` も同じ扱いです
- **symlink の向こう側を編集しない。**共有元を書き換えると、参照している全プロジェクトに
  即座に効きます。コミットもデプロイも挟まずに反映されます
- 秘密情報はリポジトリにもクライアント側コードにも入れません。ブラウザに載ったものは
  公開された時点で公開情報です
- **`TODO(依頼者確認)` を推測で埋めない。**確定情報はこの文書の「いま決まっていること」と
  `docs/design-docs/content-structure-first-draft.md`を正とします
- **公開（push / デプロイ）は自動で走らせないでください。**依頼者の確認が済むまでしません

## Tool-specific setup

共有: `harness/` `scripts/` `tools/` `.githooks/` `docs/`。
形式が違うため意図的に統一していないもの:

- Claude Code: `.claude/settings.json`（権限。**書き込み権限は
  `.claude/settings.local.json` へ。committed 側には入れない**）、
  `.claude/agents/harness-checker.md`（読み取り専用チェッカー）、
  `.claude/skills/design-comp/`（カンプの入口。手順は持たず、道具の割り当てだけ）、
  `.claude/launch.json`（静的サーバ）
- Codex: `.codex/config.toml`、`.codex/agents/harness-checker.toml`
